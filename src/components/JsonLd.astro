---
/**
 * 通用 JSON-LD 注入器
 * 用法：<JsonLd schema={ {...} } /> 或传入数组
 */
interface Props {
  schema: Record<string, unknown> | Record<string, unknown>[];
}
const { schema } = Astro.props;
const json = JSON.stringify(schema);
---
<script type="application/ld+json" set:html={json} is:inline />
